import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shareefsons.businessmanager',
  appName: 'Shareef Sons Business Manager',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
